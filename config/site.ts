export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  /** Short name for links, tooltips, and document title template suffix. */
  name: "Lodestar",
  /** Default `<title>` on the home page and main hero heading. */
  brandTitle:
    "Lodestar: A beautiful and intuitive front end for Bitmagnet.",
  /** Meta description (Open Graph, etc.). */
  description:
    "A beautiful and intuitive front end for Bitmagnet — search and explore your indexer with a polished UI.",
};
